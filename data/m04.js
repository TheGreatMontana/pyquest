/* Модуль 4 — Циклы for */
COURSE_DATA.m04 = {
  theory: [
    {
      title: '🔁 Зачем нужны циклы',
      blocks: [
        '<p>Представь: нужно вывести «Привет» 100 раз. Писать 100 строк <code>print</code>? Нет! Для повторений есть <b>циклы</b>:</p>',
        { run: "for i in range(5):\n    print('Привет номер', i)" },
        '<p>Обрати внимание на <b>отступ</b> (4 пробела): всё, что с отступом после двоеточия, — это <b>тело цикла</b>, оно повторяется. Отступы в Python — не украшение, а закон!</p>',
      ],
    },
    {
      title: '🔢 range() — генератор чисел',
      blocks: [
        '<p><code>range()</code> создаёт последовательность чисел. Три варианта:</p>',
        { run: "for i in range(4):        # 0,1,2,3\n    print(i, end=' ')\nprint()\nfor i in range(2, 6):     # 2,3,4,5\n    print(i, end=' ')\nprint()\nfor i in range(0, 10, 2): # с шагом 2\n    print(i, end=' ')" },
        '<p>Запомни: <code>range(n)</code> идёт от 0 до <b>n-1</b> (правая граница не включается — как в срезах). А <code>end=\' \'</code> в print заменяет перенос строки на пробел.</p>',
      ],
    },
    {
      title: '📋 Перебор списков',
      blocks: [
        '<p>Цикл <code>for</code> умеет проходить по любому списку — элемент за элементом:</p>',
        { run: "heroes = ['Марио', 'Соник', 'Кратос']\nfor hero in heroes:\n    print(f'{hero} готов к бою!')" },
        '<p>Если нужен и номер, и элемент — используй <code>enumerate()</code>:</p>',
        { run: "podium = ['Золото', 'Серебро', 'Бронза']\nfor i, prize in enumerate(podium, 1):\n    print(f'{i} место: {prize}')" },
      ],
    },
    {
      title: '➕ Накопление результата',
      blocks: [
        '<p>Классический приём: создаём переменную-копилку до цикла и пополняем её внутри:</p>',
        { run: 'total = 0\nfor n in [10, 25, 5, 60]:\n    total = total + n\nprint(f\'Сумма: {total}\')' },
        '<p>Сокращение: <code>total += n</code> — то же самое, что <code>total = total + n</code>. Так же копят строки и списки:</p>',
        { run: "result = []\nfor n in range(1, 6):\n    result.append(n * n)\nprint(result)  # квадраты чисел" },
      ],
    },
    {
      title: '⏭️ break и continue',
      blocks: [
        '<p><code>break</code> — выйти из цикла немедленно. <code>continue</code> — пропустить текущий шаг и перейти к следующему:</p>',
        { run: "for n in [3, 7, 12, 5, 8]:\n    if n > 10:\n        print('Нашёл число больше 10:', n)\n        break\n    print('проверяю', n)" },
        { run: "for n in range(1, 8):\n    if n % 2 == 0:\n        continue  # пропускаем чётные\n    print(n, end=' ')" },
        '<p>Вложенные циклы тоже возможны — цикл внутри цикла (например, для таблицы умножения).</p>',
      ],
    },
  ],

  quiz: [
    { q: 'Сколько раз выполнится тело цикла <code>for i in range(3):</code>?', options: ['2', '3', '4', 'Бесконечно'], a: 1, explain: '<code>range(3)</code> — это 0, 1, 2 — три повторения.' },
    { q: 'Какие числа даст <code>range(1, 4)</code>?', options: ['1, 2, 3, 4', '1, 2, 3', '0, 1, 2, 3', '2, 3, 4'], a: 1, explain: 'От 1 до 4, не включая 4: числа 1, 2, 3.' },
    { q: 'Что выведет код?', code: "total = 0\nfor n in [1, 2, 3]:\n    total += n\nprint(total)", options: ['123', '6', '3', '0'], a: 1, explain: '<code>total += n</code> прибавляет каждый элемент: 1+2+3 = 6.' },
    { q: 'Что делает <code>break</code>?', options: ['Пропускает один шаг', 'Останавливает программу', 'Немедленно выходит из цикла', 'Перезапускает цикл'], a: 2, explain: '<code>break</code> полностью прерывает цикл, программа идёт дальше после него.' },
    { q: 'Что выведет код?', code: "for x in 'куб':\n    print(x)", options: ['куб', 'к у б (в столбик)', 'Ошибку', 'x x x'], a: 1, explain: 'Цикл for перебирает и строки — по одной букве: к, у, б.' },
    { q: 'Почему этот код выдаст ошибку?', code: 'for i in range(3):\nprint(i)', broken: true, options: ['range написан неверно', 'Нет отступа у print', 'Забыли скобки', 'Ошибки нет'], a: 1, explain: 'Тело цикла обязано иметь отступ. Без отступа Python не поймёт, что повторять.' },
    { q: 'Что делает continue?', options: ['Выходит из цикла', 'Пропускает текущий шаг и идёт к следующему', 'Повторяет шаг ещё раз', 'Ничего'], a: 1, explain: '<code>continue</code> пропускает остаток тела цикла и переходит к следующей итерации.' },
  ],

  tasks: [
    {
      id: 't1', title: 'Обратный отсчёт', brief: 'range с шагом',
      desc: '<p>Выведи обратный отсчёт от 5 до 1, а потом строку <code>Пуск!</code></p><pre class="code">5\n4\n3\n2\n1\nПуск!</pre>',
      starter: '# range может идти в обратную сторону: range(старт, стоп, -1)\n',
      tests: "lines = [l.strip() for l in _stdout.strip().splitlines()]\nassert lines == ['5', '4', '3', '2', '1', 'Пуск!'], 'Ожидалось 5 4 3 2 1 Пуск! в столбик. Получилось: ' + str(lines)",
      hint: 'for i in range(5, 0, -1): print(i) — а после цикла (без отступа!) print("Пуск!")',
    },
    {
      id: 't2', title: 'Сумма чисел до 100', brief: 'Накопление в цикле',
      desc: '<p>Посчитай сумму всех чисел от 1 до 100 включительно с помощью цикла и выведи её. (Спойлер: получится 5050 — это задача, которую Гаусс решил в уме в 7 лет!)</p>',
      starter: 'total = 0\n# цикл от 1 до 100 включительно\n',
      tests: "assert '5050' in _stdout, 'Должно получиться 5050. Проверь: range(1, 101) - сотня включается только так!'",
      hint: 'for n in range(1, 101): total += n. Обрати внимание: 101, а не 100 — правая граница не включается.',
    },
    {
      id: 't3', title: 'Начисление XP', brief: 'Перебор списка + условие',
      desc: '<p>Дан список очков за раунды. Выведи для каждого раунда строку <code>Раунд N: X очков</code> (нумерация с 1), а в конце — <code>Итого: сумма</code>.</p><pre class="code">Раунд 1: 30 очков\nРаунд 2: 45 очков\nРаунд 3: 25 очков\nИтого: 100</pre>',
      starter: 'rounds = [30, 45, 25]\n# enumerate(rounds, 1) даст пары (номер, очки)\n',
      tests: "assert 'Раунд 1: 30 очков' in _stdout, 'Нужна строка: Раунд 1: 30 очков'\nassert 'Раунд 2: 45 очков' in _stdout, 'Нужна строка: Раунд 2: 45 очков'\nassert 'Раунд 3: 25 очков' in _stdout, 'Нужна строка: Раунд 3: 25 очков'\nassert 'Итого: 100' in _stdout, 'В конце нужна строка: Итого: 100'",
      hint: "for i, score in enumerate(rounds, 1): print(f'Раунд {i}: {score} очков'), потом print(f'Итого: {sum(rounds)}')",
    },
  ],

  exam: {
    time: 540,
    questions: [
      { q: 'Что выведет код?', code: "for i in range(2, 5):\n    print(i, end=' ')", options: ['2 3 4 5', '2 3 4', '3 4 5', '2 3'], a: 1, explain: '' },
      { q: 'Что выведет код?', code: "s = 0\nfor n in range(4):\n    s += n\nprint(s)", options: ['10', '6', '4', '0'], a: 1, explain: '' },
      { q: 'Что выведет код?', code: "for n in [5, 8, 2]:\n    if n == 8:\n        break\n    print(n)", options: ['5', '5 8', '5 2', '8'], a: 0, explain: '' },
      { q: 'Что выведет код?', code: "count = 0\nfor x in ['a', 'b', 'c', 'd']:\n    count += 1\nprint(count)", options: ['3', '4', '0', 'abcd'], a: 1, explain: '' },
      { q: 'Какой range даст числа 10, 20, 30?', options: ['range(10, 30)', 'range(10, 31, 10)', 'range(10, 30, 10)', 'range(30, 10)'], a: 1, explain: '' },
      { q: 'Что выведет код?', code: "for i in range(3):\n    for j in range(2):\n        print('*', end='')\n    print()", options: ['*** (одна строка)', '** три раза в столбик', '*** два раза', '******'], a: 1, explain: '' },
    ],
    task: {
      title: 'Чётный фильтр',
      desc: '<p>Дан список чисел. С помощью цикла собери в новый список <code>evens</code> только чётные числа (<code>n % 2 == 0</code>) и выведи его.</p><p>Ожидаемый вывод: <code>[4, 8, 10]</code></p>',
      starter: 'nums = [3, 4, 7, 8, 10, 15]\nevens = []\n# цикл + условие + append\n',
      tests: "assert evens == [4, 8, 10], 'В evens должны попасть 4, 8, 10. Сейчас: ' + str(evens)\nassert '[4, 8, 10]' in _stdout, 'Выведи список: print(evens)'",
    },
  },
};

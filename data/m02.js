/* Модуль 2 — Переменные и типы данных */
COURSE_DATA.m02 = {
  theory: [
    {
      title: '📦 Переменные — коробки с данными',
      blocks: [
        '<p><b>Переменная</b> — это имя, к которому привязано значение. Создаётся знаком <code>=</code>:</p>',
        { run: "name = 'Азиз'\nage = 20\nprint(name)\nprint(age)" },
        '<p>Значение можно менять в любой момент — поэтому она и «переменная»:</p>',
        { run: 'score = 10\nprint(score)\nscore = score + 5\nprint(score)' },
        '<p>Правила имён: только буквы, цифры и <code>_</code>; не начинать с цифры; регистр важен (<code>Name</code> и <code>name</code> — разные). Пиши имена со смыслом: <code>user_age</code>, а не <code>x</code>.</p>',
      ],
    },
    {
      title: '🔢 Числа: int и float',
      blocks: [
        '<p><code>int</code> — целые числа, <code>float</code> — дробные (с точкой). С ними работают все математические операции:</p>',
        { run: 'a = 7\nb = 2\nprint(a + b)   # сложение\nprint(a - b)   # вычитание\nprint(a * b)   # умножение\nprint(a / b)   # деление - всегда float!\nprint(a // b)  # целочисленное деление\nprint(a % b)   # остаток от деления\nprint(a ** b)  # степень' },
        '<p>Запомни: обычное деление <code>/</code> всегда даёт <code>float</code> (7 / 2 = 3.5), а <code>//</code> отбрасывает дробную часть (7 // 2 = 3). Остаток <code>%</code> супер-полезен: чётность числа проверяют как <code>n % 2 == 0</code>.</p>',
      ],
    },
    {
      title: '📝 Строки (str)',
      blocks: [
        '<p><b>Строка</b> — любой текст в кавычках. Строки можно складывать (конкатенация) и умножать:</p>',
        { run: "first = 'Py'\nsecond = 'Quest'\nprint(first + second)\nprint('ха' * 3)\nprint(len('Python'))  # len() - длина строки" },
        '<p>Полезные методы строк:</p>',
        { run: "s = 'python это сила'\nprint(s.upper())       # ВСЕ БОЛЬШИЕ\nprint(s.title())       # Каждое Слово С Большой\nprint(s.replace('сила', 'мощь'))\nprint('  пробелы  '.strip())  # обрезать пробелы" },
      ],
    },
    {
      title: '✨ f-строки — магия вставки',
      blocks: [
        '<p><b>f-строка</b> — самый удобный способ вставить переменную в текст. Ставишь <code>f</code> перед кавычками и переменные в фигурных скобках:</p>',
        { run: "name = 'Азиз'\nage = 20\nprint(f'Меня зовут {name}, мне {age} лет')\nprint(f'Через 5 лет мне будет {age + 5}')" },
        '<p>Внутри <code>{}</code> можно писать любые выражения — Python их вычислит. Это используется постоянно, запомни хорошо!</p>',
      ],
    },
    {
      title: '🎭 Типы, type() и преобразования',
      blocks: [
        '<p>У каждого значения есть тип. Узнать его можно функцией <code>type()</code>:</p>',
        { run: "print(type(42))\nprint(type(3.14))\nprint(type('текст'))\nprint(type(True))" },
        '<p><code>bool</code> — логический тип: только <code>True</code> или <code>False</code>. Типы можно преобразовывать:</p>',
        { run: "x = '10'          # это строка!\nprint(x + '5')    # строки склеиваются: 105\ny = int(x)        # теперь число\nprint(y + 5)      # 15\nprint(str(99) + '%')  # число -> строка\nprint(float('2.5') * 2)" },
      ],
    },
    {
      title: '⌨️ input() — общение с пользователем',
      blocks: [
        '<p><code>input()</code> останавливает программу и ждёт, что введёт пользователь. Результат — <b>всегда строка</b>:</p>',
        { code: "name = input('Как тебя зовут? ')\nprint(f'Привет, {name}!')" },
        '<p>⚠️ Главная ловушка: если нужен возраст или число — оборачивай в <code>int()</code>:</p>',
        { code: "age = int(input('Сколько лет? '))\nprint(f'Через год тебе будет {age + 1}')" },
        '<p>В задачах этого сайта ввод подставляется автоматически — просто используй <code>input()</code> как обычно.</p>',
      ],
    },
  ],

  quiz: [
    { q: 'Какой тип у значения <code>3.14</code>?', options: ['int', 'float', 'str', 'bool'], a: 1, explain: 'Числа с точкой — это <code>float</code> (дробные).' },
    { q: 'Что выведет код?', code: "x = '5'\nprint(x + x)", options: ['10', '55', 'Ошибку', '5 + 5'], a: 1, explain: '<code>x</code> — строка, а строки складываются склеиванием: «5» + «5» = «55».' },
    { q: 'Что вернёт <code>10 % 3</code>?', options: ['3', '3.33', '1', '0'], a: 2, explain: '<code>%</code> — остаток от деления: 10 = 3·3 + <b>1</b>.' },
    { q: 'Как правильно вставить переменную name в строку?', options: ["print('Привет, name')", "print(f'Привет, {name}')", "print('Привет, {name}')", "print(f'Привет, name')"], a: 1, explain: 'Нужна буква <code>f</code> перед кавычками И фигурные скобки: <code>f\'Привет, {name}\'</code>.' },
    { q: 'Что всегда возвращает input()?', options: ['Число (int)', 'Строку (str)', 'Что введёт пользователь: число или строку', 'bool'], a: 1, explain: '<code>input()</code> всегда возвращает строку. Даже если ввели 25 — это строка «25». Для числа нужен <code>int()</code>.' },
    { q: 'Что выведет код?', code: 'print(7 // 2)', options: ['3.5', '3', '4', '1'], a: 1, explain: '<code>//</code> — целочисленное деление: дробная часть отбрасывается, остаётся 3.' },
    { q: 'Какое имя переменной НЕправильное?', options: ['user_name', '_score', '2players', 'totalXP'], a: 2, explain: 'Имя переменной не может начинаться с цифры. <code>2players</code> вызовет ошибку.' },
  ],

  tasks: [
    {
      id: 't1', title: 'Карточка игрока', brief: 'Переменные + f-строка',
      desc: '<p>Создай переменные <code>name</code> со значением <code>"Азиз"</code> и <code>level</code> со значением <code>5</code>. Выведи с помощью f-строки: <code>Игрок Азиз, уровень 5</code></p>',
      starter: '# Создай переменные name и level\n# Выведи текст через f-строку\n',
      tests: "assert 'name' in dir(), 'Создай переменную name'\nassert name == 'Азиз', 'name должна быть равна: Азиз'\nassert 'level' in dir(), 'Создай переменную level'\nassert level == 5, 'level должна быть равна 5'\nassert _stdout.strip() == 'Игрок Азиз, уровень 5', 'Ожидался вывод: Игрок Азиз, уровень 5. Получилось: ' + repr(_stdout.strip())",
      hint: "name = 'Азиз', level = 5, потом print(f'Игрок {name}, уровень {level}')",
    },
    {
      id: 't2', title: 'Сколько секунд в сутках?', brief: 'Арифметика',
      desc: '<p>Посчитай с помощью Python, сколько секунд в сутках (24 часа × 60 минут × 60 секунд), и выведи результат одним числом.</p>',
      starter: '# Перемножь 24, 60 и 60\n',
      tests: "assert '86400' in _stdout, 'Ожидалось число 86400. Считай через print(24 * 60 * 60)'",
      hint: 'print(24 * 60 * 60) — пусть Python считает сам, в этом его сила!',
    },
    {
      id: 't3', title: 'Удвоитель чисел', brief: 'input() + int()',
      desc: '<p>Программа спрашивает число через <code>input()</code>, а потом выводит его удвоенное значение.</p><p>Например, при вводе <code>21</code> вывод: <code>42</code></p><p><i>Ввод подставится автоматически при проверке.</i></p>',
      starter: "n = input('Введи число: ')\n# Преврати n в int и выведи n * 2\n",
      stdin: ['21'],
      tests: "assert '42' in _stdout, 'При вводе 21 должно вывестись 42. Не забудь int()!'\nassert '2121' not in _stdout, 'Вывелось 2121 - ты умножил СТРОКУ. Сначала int(n)!'",
      hint: 'n = int(input(...)) — тогда n станет числом и n * 2 посчитается математически.',
    },
  ],

  exam: {
    time: 480,
    questions: [
      { q: 'Что выведет код?', code: "a = 3\nb = a * 2\na = 10\nprint(b)", options: ['6', '20', '10', 'Ошибку'], a: 0, explain: '' },
      { q: 'Тип значения <code>True</code>?', options: ['str', 'int', 'bool', 'float'], a: 2, explain: '' },
      { q: 'Что выведет код?', code: "print('7' + str(3))", options: ['10', '73', 'Ошибку', '7 3'], a: 1, explain: '' },
      { q: 'Как получить остаток от деления 17 на 5?', options: ['17 / 5', '17 // 5', '17 % 5', '17 ** 5'], a: 2, explain: '' },
      { q: 'Что выведет код?', code: "x = int('4')\nprint(x * 3)", options: ['444', '12', 'Ошибку', '43'], a: 1, explain: '' },
      { q: 'Что выведет f-строка?', code: "lvl = 9\nprint(f'Уровень: {lvl + 1}')", options: ['Уровень: {lvl + 1}', 'Уровень: 9 + 1', 'Уровень: 10', 'Ошибку'], a: 2, explain: '' },
    ],
    task: {
      title: 'Приветствие по имени и возрасту',
      desc: '<p>Программа читает через <code>input()</code> сначала имя, потом возраст (числом), и выводит: <code>Имя, через 10 лет тебе будет X</code></p><p>Например, для ввода <code>Тимур</code> и <code>15</code>: <code>Тимур, через 10 лет тебе будет 25</code></p>',
      starter: "name = input('Имя: ')\nage = input('Возраст: ')\n# твой код\n",
      stdin: ['Тимур', '15'],
      tests: "assert 'Тимур, через 10 лет тебе будет 25' in _stdout, 'Ожидалась строка: Тимур, через 10 лет тебе будет 25. Проверь int() и f-строку!'",
    },
  },
};
